/**
 * logging_middleware_local/logger.ts
 * Local shim that re-exports the shared logging middleware for use in the frontend.
 */
export { Log, Log as default } from "../../logging_middleware/src/logger";
